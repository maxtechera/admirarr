version: "2"

linters:
  default: standard
  disable:
    # errcheck: widespread unchecked errors in test helpers and CLI code;
    # pre-existing throughout codebase, tracked separately from this ticket.
    - errcheck
