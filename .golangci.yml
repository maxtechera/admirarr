version: "2"

linters:
  # Minimal: only enable linters with no pre-existing failures.
  # Pre-existing lint debt (errcheck, govet, ineffassign, staticcheck,
  # unused) tracked separately — not blocking CI gate.
  default: none
  enable:
    - misspell

formatters:
  enable:
    - gofmt
