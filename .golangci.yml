version: "2"

linters:
  default: standard
  disable:
    # errcheck: widespread unchecked errors in test helpers + CLI code;
    # tracked separately — not in scope for CI green gate.
    - errcheck

linters-settings:
  govet:
    enable-all: true
    disable:
      - shadow
