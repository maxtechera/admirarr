version: "2"

linters:
  # Start with no linters and only enable what we know works cleanly.
  # Pre-existing lint debt across the codebase is tracked separately.
  default: none
  enable:
    - gofmt
    - misspell
